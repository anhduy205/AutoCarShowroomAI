@model Showroom.Web.Models.PublicCarsIndexViewModel

@{
    ViewData["Title"] = "Xe Ã´ tÃ´";
    var hasFilters = !string.IsNullOrWhiteSpace(Model.Query)
        || Model.BrandId is not null
        || !string.IsNullOrWhiteSpace(Model.Type)
        || Model.MinPrice is not null
        || Model.MaxPrice is not null
        || Model.YearFrom is not null
        || Model.YearTo is not null;
    var selectedBrand = Model.BrandId is null
        ? null
        : Model.BrandOptions.FirstOrDefault(option => option.Value == Model.BrandId.Value.ToString())?.Text;
}

<section class="market-page">
    <form method="get" action="/cars" class="market-shell">
        <aside class="market-filter" aria-label="Bá»™ lá»c xe">
            <h2>Bá»˜ Lá»ŒC</h2>

            <div class="market-filter-section open">
                <button type="button">HÃƒNG XE, DÃ’NG XE <span>âŒ„</span></button>
                <select class="form-select" name="brandId">
                    <option value="">Táº¥t cáº£ hÃ£ng</option>
                    @foreach (var option in Model.BrandOptions)
                    {
                        var isSelected = Model.BrandId is not null && option.Value == Model.BrandId.Value.ToString();
                        if (isSelected)
                        {
                            <option value="@option.Value" selected>@option.Text</option>
                        }
                        else
                        {
                            <option value="@option.Value">@option.Text</option>
                        }
                    }
                </select>
            </div>

            <div class="market-filter-section open">
                <button type="button">KIá»‚U DÃNG <span>âŒ„</span></button>
                <select class="form-select" name="type">
                    @foreach (var option in Model.TypeOptions)
                    {
                        if (option.Selected)
                        {
                            <option value="@option.Value" selected>@option.Text</option>
                        }
                        else
                        {
                            <option value="@option.Value">@option.Text</option>
                        }
                    }
                </select>
            </div>

            <div class="market-filter-section open">
                <button type="button">GIÃ <span>âŒ„</span></button>
                <div class="market-range-grid">
                    <input class="form-control" type="number" min="0" step="1000000" name="minPrice" value="@(Model.MinPrice?.ToString() ?? string.Empty)" placeholder="Tá»«" />
                    <input class="form-control" type="number" min="0" step="1000000" name="maxPrice" value="@(Model.MaxPrice?.ToString() ?? string.Empty)" placeholder="Äáº¿n" />
                </div>
            </div>

            <div class="market-filter-section open">
                <button type="button">NÄ‚M Sáº¢N XUáº¤T <span>âŒ„</span></button>
                <div class="market-range-grid">
                    <input class="form-control" type="number" min="1900" max="2100" step="1" name="yearFrom" value="@(Model.YearFrom?.ToString() ?? string.Empty)" placeholder="Tá»«" />
                    <input class="form-control" type="number" min="1900" max="2100" step="1" name="yearTo" value="@(Model.YearTo?.ToString() ?? string.Empty)" placeholder="Äáº¿n" />
                </div>
            </div>


            <div class="market-filter-actions">
                <button class="btn btn-primary" type="submit">Ãp dá»¥ng</button>
                <a class="btn btn-outline-secondary" href="/cars">XÃ³a lá»c</a>
            </div>
        </aside>

        <main class="market-results">
            <div class="market-search">
                <span aria-hidden="true">âŒ•</span>
                <input class="form-control"
                       name="q"
                       value="@Model.Query"
                       placeholder="TÃ¬m kiáº¿m xe theo HÃ£ng xe, DÃ²ng xe hoáº·c Tá»« khÃ³a" />
                <button class="btn btn-primary" type="submit">TÃ¬m kiáº¿m</button>
            </div>

            <div class="market-active-filters">
                <strong>Bá»˜ Lá»ŒC</strong>
                @if (hasFilters)
                {
                    @if (!string.IsNullOrWhiteSpace(Model.Query))
                    {
                        <span>@Model.Query</span>
                    }
                    @if (!string.IsNullOrWhiteSpace(selectedBrand))
                    {
                        <span>@selectedBrand</span>
                    }
                    @if (!string.IsNullOrWhiteSpace(Model.Type))
                    {
                        <span>@Model.Type</span>
                    }
                    @if (Model.MinPrice is not null || Model.MaxPrice is not null)
                    {
                        <span>
                            @(Model.MinPrice?.ToString("N0") ?? "0") Ä‘áº¿n @(Model.MaxPrice?.ToString("N0") ?? "khÃ´ng giá»›i háº¡n")
                        </span>
                    }
                }
                else
                {
                    <span>Táº¥t cáº£ xe</span>
                }
            </div>

            <div class="market-result-heading">

                <label class="market-sort">
                    <span>Sáº®P Xáº¾P:</span>
                    <select class="form-select" name="sort" onchange="this.form.submit()">
                        @if (Model.Sort == PublicCarSortCatalog.Relevance)
                        {
                            <option value="@PublicCarSortCatalog.Relevance" selected>LiÃªn quan nháº¥t</option>
                        }
                        else
                        {
                            <option value="@PublicCarSortCatalog.Relevance">LiÃªn quan nháº¥t</option>
                        }

                        @if (Model.Sort == PublicCarSortCatalog.Newest)
                        {
                            <option value="@PublicCarSortCatalog.Newest" selected>Má»›i nháº¥t</option>
                        }
                        else
                        {
                            <option value="@PublicCarSortCatalog.Newest">Má»›i nháº¥t</option>
                        }

                        @if (Model.Sort == PublicCarSortCatalog.PriceAsc)
                        {
                            <option value="@PublicCarSortCatalog.PriceAsc" selected>GiÃ¡ tháº¥p Ä‘áº¿n cao</option>
                        }
                        else
                        {
                            <option value="@PublicCarSortCatalog.PriceAsc">GiÃ¡ tháº¥p Ä‘áº¿n cao</option>
                        }

                        @if (Model.Sort == PublicCarSortCatalog.PriceDesc)
                        {
                            <option value="@PublicCarSortCatalog.PriceDesc" selected>GiÃ¡ cao Ä‘áº¿n tháº¥p</option>
                        }
                        else
                        {
                            <option value="@PublicCarSortCatalog.PriceDesc">GiÃ¡ cao Ä‘áº¿n tháº¥p</option>
                        }

                        @if (Model.Sort == PublicCarSortCatalog.YearDesc)
                        {
                            <option value="@PublicCarSortCatalog.YearDesc" selected>NÄƒm má»›i nháº¥t</option>
                        }
                        else
                        {
                            <option value="@PublicCarSortCatalog.YearDesc">NÄƒm má»›i nháº¥t</option>
                        }
                    </select>
                </label>
            </div>

            @if (Model.Cars.Count == 0)
            {
                <article class="market-empty">
                    KhÃ´ng tÃ¬m tháº¥y xe phÃ¹ há»£p. HÃ£y thá»­ thay Ä‘á»•i bá»™ lá»c.
                </article>
            }
            else
            {
                <div class="market-card-grid">
                    @foreach (var car in Model.Cars)
                    {
                        <a class="market-car-card" href="/cars/@car.Id">
                            <div class="market-car-image">
                                @if (!string.IsNullOrWhiteSpace(car.ThumbnailUrl))
                                {
                                    <img src="@car.ThumbnailUrl" alt="áº¢nh @car.Name" loading="lazy" />
                                }
                                else
                                {
                                    <div class="market-car-placeholder">
                                        <strong>@car.BrandName</strong>
                                        <span>@car.Type</span>
                                    </div>
                                }
                                <span class="market-image-dots"></span>
                            </div>
                            <div class="market-car-body">
                                <h2>@car.Name</h2>
                                <div class="market-car-specs">
                                    <span>@(car.Year?.ToString() ?? "No data")</span>
                                    <span>@(string.IsNullOrWhiteSpace(car.Type) ? "Xe má»›i" : car.Type)</span>
                                    <span>@(string.IsNullOrWhiteSpace(car.Color) ? "MÃ u Ä‘ang cáº­p nháº­t" : car.Color)</span>
                                    <span>@car.Status</span>
                                </div>
                                <div class="market-car-price">@car.Price.ToString("N0") VNÄ</div>
                                <div class="market-car-location">Auto Car Showroom</div>
                            </div>
                        </a>
                    }
                </div>
            }
        </main>
    </form>
</section>
